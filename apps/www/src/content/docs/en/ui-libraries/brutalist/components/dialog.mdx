---
title: 'Brutalist Dialog'
description: 'Industrial Neo-Brutalist dialog and dismissal controls'
---

import { PrototypePreviewer } from '@/components/PrototypePreviewer';
import BrutalistPageStyle from '@/components/BrutalistPageStyle.astro';

<BrutalistPageStyle />

# Dialog

The panel keeps Base Dialog semantics and command wiring while using an industrial poster treatment.

- The opener is an electric-blue hard-shadow control.
- The top-right X is a dedicated acid-yellow square control, not a borderless icon.
- Hover changes the X to destructive red and increases its hard shadow.
- Press advances the control `1px` in both axes and removes its shadow.
- Footer dismiss actions use the same explicit close semantics; they must not masquerade as neutral text links.
- Focus remains a visible hard outline; the accessible `Close` name is preserved.

<div style="--pui-radius:0;--pui-background:#f4f1ea;--pui-secondary-background:#fff;--pui-foreground:#000;--pui-border:#000;--pui-electric-blue:#2563EB;--pui-acid-yellow:#EFFF00;background:#f4f1ea;color:#000;border:2px solid var(--pui-foreground);box-shadow:3px 3px 0 var(--pui-foreground);padding:1rem;">
  <PrototypePreviewer demoId="demo-brutalist-dialog" initialRuntime="wc" />
</div>

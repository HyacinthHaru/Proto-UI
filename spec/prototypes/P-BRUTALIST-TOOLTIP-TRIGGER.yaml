id: P-BRUTALIST-TOOLTIP-TRIGGER
type: prototype
title: Brutalist Tooltip Trigger projection
status: draft
since: 0.2.0-rc.7
summary: Brutalist Trigger inherits Base interaction and accessibility without adding style or behavior.
statement:
  en: Brutalist Trigger consumes Base Trigger and preserves its disabled, hover, focus, focusSelf, interaction-intent, and describedBy surfaces without adding another protocol or feedback style.
  zh-CN: Brutalist Trigger 消费 Base Trigger，保留 disabled、hover、focus、focusSelf、interaction-intent 与 describedBy surfaces，不增加另一套 protocol 或 feedback style。
criteria:
  - id: P-BRUTALIST-TOOLTIP-TRIGGER-PURE-INHERITANCE
    text:
      en: Trigger must consume asTooltipTrigger, preserve the Base behavior surface, and add no protocol or style surface.
      zh-CN: Trigger 必须消费 asTooltipTrigger、保留 Base behavior surface，且不增加 protocol 或 style surface。
sources: [{ path: packages/prototypes/brutalist/src/tooltip/trigger.proto.ts }]
revisions:
  - version: 0.2.0-rc.7
    change: introduced
    summary: Introduced the unstyled Brutalist Tooltip Trigger projection.
tags: [prototype, brutalist, tooltip, trigger]
inherits:
  prototypes: [{ id: P-BASE-TOOLTIP-TRIGGER }]
dependsOn:
  contracts: [C-CORE-SYNTAX-0003, C-AS-HOOK-0001]
verifies:
  tests: [T-BRUTALIST-TOOLTIP-0001]

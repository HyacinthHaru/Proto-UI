# 0.2.0-rc.7 release train 当前 main 刷新记录

日期：2026-08-11

## 背景

Draft PR #355 最初基于 `main` 的 `7c47b620`，在 2026-08-01 记录 39 个公开 package、Brutalist 非 launch commitment 与尚未覆盖后续合入项的双语 release notes。此后 `main` 继续合入 #356、#357、#358、#359、#360、#361、#362、#363 与 #338，并到达 `ce119b0d`。旧 release 分支与当前 `main` 已出现 11 个内容冲突，且其中部分短期方向已被后续评审取代。

本记录描述基于当前 `main` 重新建立 rc.7 准备差异的事实与边界。它不替代发布后不可变证据，也不宣称 rc.7 已经可安装。

## 当前 source-of-truth 状态

- `VERSION`、全部公开 package manifest、`V-PROTO-UI-0007` 与 launch governance 已对齐 `0.2.0-rc.7`。
- 当前 deterministic BOM 包含 40 个公开 package；新增的第 40 个 identity 是 `@proto.ui/module-text-control`。
- `@proto.ui/prototypes-brutalist` 已进入 launch commitment，公开 family 与 CLI `add`/preset closure 覆盖 Button、Badge、Card、Toggle、Switch、Tabs、Hover Card、Dropdown、Select、Dialog、Scroll Area、Separator、Skeleton 与 Textarea。
- Badge 与 Card 保持 direct styled-only `draft` prototype，不重新引入空的 Base carrier；Scroll、Tooltip 与其他未激活 catalog slice 继续按各自实体生命周期呈现。
- 双语 release notes 已合并旧 #355 中仍有效的 Scroll、Tooltip 与 canonical repository 修复，并补入后续 main 的 Textarea、Live/Async Region、Badge/Card、focus/ThemeProvider/demo hardening、Pagefind 搜索、UI library showcase、information-flow 文档和 DCO/provenance governance。

## 旧 draft 分支的处理方式

旧 #355 的三个准备提交不应直接叠加到当前 `main`：

- 39-package BOM、Brutalist 非 launch commitment 与 CLI `add` 延后已经过时；
- Brutalist README/manifest、metadata sync、launch policy、surface map 与 V entity 已在后续 PR 中获得更完整的 source-of-truth 更新；
- 当前 BOM 应继续由 `pnpm release:bom` 生成，不能用旧分支版本手工覆盖；
- 旧准备记录中 39-package publication runbook 不再适用于当前 40-package workspace set。

因此 release PR 应从当前 `origin/main` 重建最小差异，只保留当前双语 release notes、V entity refresh 与本记录，而不是机械解决旧分支冲突或恢复旧投影。

## Registry readiness 核对

2026-08-11 的初始公开 registry 读取结果：

- `@proto.ui/module-scroll`：仅存在 deprecated identity bootstrap `0.0.0-bootstrap.0`；
- `@proto.ui/prototypes-brutalist`：仅存在 deprecated identity bootstrap `0.0.0-bootstrap.0`；
- `@proto.ui/module-text-control`：npm 初始返回 `E404`，identity 尚不存在；
- 本机 `npm whoami --registry=https://registry.npmjs.org/` 初始处于未认证状态（registry 返回 `E401`，CLI 也可表述为 `ENEEDAUTH`），没有可用于 maintainer bootstrap 的 npm 登录态。

维护者随后通过 npm 官方网页登录与 2FA 完成 bootstrap：

- 发布 `@proto.ui/module-text-control@0.0.0-bootstrap.0`，tarball 仅包含 `package.json` 与 README，不含 Proto UI runtime code；
- 使用 `bootstrap` tag 并立即将该版本标记 deprecated；npm 在 package 只有一个版本时同时保留临时 `latest`，但没有占用 `next`；
- Trusted Publisher 已绑定到 `Proto-UI/Proto-UI`、`release-packages.yml` 与 `npm` environment，允许受保护 workflow 执行 `npm publish`；trust ID 为 `a7df67ae-c8be-4445-b52c-c9594aa38f84`；
- `release:registry:check` 随后确认 40 个公开 package identity 全部 ready。

## 验证与发布边界

Registry blocker 清除后，完整 `release:rehearse` 已在本分支顺序通过：release identity/assets、catalog、types、41 条 release tests、contract types、280 个 runtime test files 与 1,244 个 tests、spec snapshot 演练、包含 Brutalist 的 8-package launch-governance scan、40/40 registry identity、40/40 package build 与 publish dry-run、React 36/40 tarball consumer、CLI 38/40 tarball consumer，以及 190-page/188-indexed production docs build。演练结束时明确输出 `no publication was performed`。

本次演练生成的临时 snapshot digest 为 `sha256:0f8c6f66092ce92b064ef828f37ba121aad7a89909e7aee1c3b8c2a3267681d7`。它只证明准备分支可生成自洽快照，不得作为未来 tag 所附不可变 snapshot digest 回填或复用。

准备 PR 合入后，真实 publication 必须从合入后的 `main` 手动运行 `release-packages.yml`，使用 `mode=publish-all`、`profile=workspace` 与正常路径下的 `resume_published=false`。只有全部 40 个 package 发布成功后，workflow 才可推进 npm `next`、创建 `v0.2.0-rc.7`、GitHub prerelease 与不可变 spec snapshot。随后再从最新 `main` 建立独立 evidence PR，核对 registry、dist-tag、tag、release assets 与 digest，并把 V entity 从 `draft` 激活。
